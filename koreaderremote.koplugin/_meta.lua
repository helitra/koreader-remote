local _ = require("gettext")

return {
    fullname = _("KOReader Remote"),
    description = _([[Control KOReader page turns and supported device settings from a phone with QR pairing and reliable Wi-Fi recovery.]]),
}
